/**
 * Vuex app module: global app settings.
 * Demo Mode removed in v1.7.0 - system now uses real Paytaca connections only.
 */

const state = {
  // Reserved for future app-level settings
}

const mutations = {
  // Reserved for future mutations
}

const actions = {
  // Reserved for future actions
}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
}
